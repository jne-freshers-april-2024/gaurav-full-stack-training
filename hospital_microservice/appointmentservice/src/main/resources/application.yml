server:
  port: 8081

spring:
  application:
    name: appointmentservice
  datasource:
    url: jdbc:mysql://localhost:3306/hospital_microservice
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  config:
    import: optional:configserver:http://localhost:8085
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka